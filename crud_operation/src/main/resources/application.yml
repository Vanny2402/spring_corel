spring:
  application:
    name: crud_operation
  datasource:
    url: jdbc:postgresql://localhost:5432/jsf_db
    username: phonshop_night
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show-sql: true
server:
  port: 8090


